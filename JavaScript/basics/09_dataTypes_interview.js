// DATATYPES INTERVIEW


// Is JS dynamically typed or statically typed?
/* JavaScript is a dynamically typed language, 
which means that data types of variables are determined 
by the value they hold at runtime and can change throughout 
the program as we assign different values to them */

// +++++++++++++++++++++++++++++++++++++++++

// DATATYPES
// Primitive Datatype ==> number, bigint, string, null, undefined, boolean, symbol
// Reference Datatype ==> (non-primitive) array, object, functions functions_(all rereference type return a function type value)

// null is also object type

// ++++++++++++ Symbol +++++++++++
const a = Symbol('123');
const b = Symbol('123');

console.log(a === b);


